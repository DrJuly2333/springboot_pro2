spring:
  #  ˝æ›ø‚≈‰÷√
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/springboot?characterEncoding=utf8&useSSl=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
  # JPA ≈‰÷√
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: update
  # Web ≈‰÷√
server:
  port: 8080
  servlet:
    context-path: /springboot
    session:
      timeout: 60
